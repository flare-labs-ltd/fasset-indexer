import { FAssetType } from "../database/entities/events/_bound"
import type { EntityManager } from "@mikro-orm/core"


/* export async function convertToUsd(em: EntityManager, amountUBA: bigint, fasset: FAssetType): Promise<bigint> {
  const
} */